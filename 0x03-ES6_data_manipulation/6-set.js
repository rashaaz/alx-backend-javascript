/**
 * setFromArray - creates a Set from an array
 * @param {Array} array - the array to create
 * @author rasha saeed <https://github.com/saeed>
 * @returns a Set containing the elements
 */
export default function setFromArray(array) {
  return new Set(array);
}
